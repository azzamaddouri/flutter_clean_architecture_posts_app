import 'package:flutter/material.dart';

class PageDetailWidget extends StatelessWidget {
  const PageDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}